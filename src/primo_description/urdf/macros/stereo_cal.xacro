<?xml version="1.0"?>
<robot xmlns:xacro="http://www.ros.org/wiki/xacro">

    <!-- parameters -->
    <xacro:property name="seecamSize" value="0.05"/>
    <xacro:property name="seecamMass" value="0.1"/>
    <xacro:property name="stereocamSize" value="0.05"/>
    <xacro:property name="stereocamMass" value="0.1"/>

    <material name="red">
        <color rgba="1 0 0 1"/>
    </material>
    <material name="green">
        <color rgba="0 1 0 1"/>
    </material>
    <material name="blue">
        <color rgba="0 0 1 1"/>
    </material>
    <material name="white">
        <color rgba="1 1 1 1"/>
    </material>

    <!-- stereo${num} joint test macro -->
    <xacro:macro name="m_stereo_cal" params="num">
        <link name="stereo${num}_link">
            <collision>
                <origin xyz="0 0 0" rpy="0 0 0"/>
                <geometry>
                    <box size="${stereocamSize} ${stereocamSize} ${stereocamSize}"/>
                </geometry>
            </collision>
            <visual>
                <origin xyz="0 0 0" rpy="0 0 0"/>
                <geometry>
                    <box size="${stereocamSize} ${stereocamSize} ${stereocamSize}"/>
                </geometry>
                <material name="red"/>
            </visual>
            <inertial>
                <mass value="${stereocamMass}"/>
                <origin xyz="0 0 0" rpy="0 0 0"/>
                <inertia 
                    ixx="1e-6" ixy="0" ixz="0" 
                    iyy="1e-6" iyz="0" 
                    izz="1e-6"/>
            </inertial>
        </link>

        <!-- Joint links -->
        <link name="stereo${num}_x_link">
            <visual>
                <geometry>
                    <box size="0.1 0.1 0.1"/>
                </geometry>
                <material name="red"/>
            </visual>
        </link>
        <link name="stereo${num}_y_link">
            <visual>
                <geometry>
                    <box size="0.1 0.1 0.1"/>
                </geometry>
                <material name="green"/>
            </visual>
        </link>
        <link name="stereo${num}_z_link">
            <visual>
                <geometry>
                    <box size="0.1 0.1 0.1"/>
                </geometry>
                <material name="blue"/>
            </visual>
        </link>
        <link name="stereo${num}_roll_link">
            <visual>
                <geometry>
                    <box size="0.1 0.1 0.1"/>
                </geometry>
                <material name="red"/>
            </visual>
        </link>
        <link name="stereo${num}_pitch_link">
            <visual>
                <geometry>
                    <box size="0.1 0.1 0.1"/>
                </geometry>
                <material name="green"/>
            </visual>
        </link>
        <link name="stereo${num}_yaw_link">
            <visual>
                <geometry>
                    <box size="0.1 0.1 0.1"/>
                </geometry>
                <material name="blue"/>
            </visual>
        </link>

        <!-- Joint test joints -->
        <joint name="stereo${num}_x_joint" type="continuous">
            <origin xyz="-.03 .20 1.81" rpy="0 0 0"/>
            <axis xyz="1 0 0"/>
            <parent link="chassis_link"/>
            <child link="stereo${num}_yaw_link"/>
        </joint>

        <!-- Roll joint -->
        <joint name="stereo${num}_joint_test_yaw" type="continuous">
            <origin xyz="0 0 0"/>
            <axis xyz="0 0 1"/>
            <parent link="stereo${num}_yaw_link"/>
            <child link="stereo${num}_pitch_link"/>
        </joint>

        <!-- Pitch joint -->
        <joint name="stereo${num}_joint_test_pitch" type="continuous">
            <origin xyz="0 0 0"/>
            <axis xyz="0 1 0"/>
            <parent link="stereo${num}_pitch_link"/>
            <child link="stereo${num}_roll_link"/>
        </joint>

        <!-- Yaw joint -->
        <joint name="stereo${num}_joint_test_roll" type="continuous">
            <origin xyz="0 0 0"/>
            <axis xyz="1 0 0"/>
            <parent link="stereo${num}_roll_link"/>
            <!-- <child link="stereo${num}_end_link"/> -->
            <child link="stereo${num}_link"/>
        </joint>
    </xacro:macro>
</robot>
